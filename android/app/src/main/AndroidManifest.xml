<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ==========================================================
         Permissions
         ========================================================== -->

    <!-- Required for Firebase, Cloudinary, Google Sign-In and any HTTP call. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- image_picker — gallery access -->
    <!-- Android 13+ (API 33): granular media permission -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <!-- Android 14+ (API 34): allow user to grant access to selected photos only -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED"/>
    <!-- Android 12 and below: legacy storage read -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>

    <!-- Optional — only declare CAMERA if the app actually launches the camera intent. -->
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- ==========================================================
         Application
         ==========================================================
         Release-mode hardening:
           - usesCleartextTraffic="false"   → HTTPS only (Firebase/Cloudinary are HTTPS).
           - allowBackup="false"            → don't sync auth tokens / secure storage.
           - fullBackupContent="false"      → block legacy auto backup.
           - dataExtractionRules            → see res/xml/data_extraction_rules.xml (Android 12+).
           - hardwareAccelerated, largeHeap → Flutter rendering.
           - enableOnBackInvokedCallback    → opt into Android 13+ predictive back gesture.
         ========================================================== -->
    <application
        android:label="قصصي"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:usesCleartextTraffic="false"
        android:enableOnBackInvokedCallback="true"
        android:requestLegacyExternalStorage="false"
        android:hardwareAccelerated="true"
        android:largeHeap="true"
        tools:targetApi="33">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Theme applied while the Flutter UI initializes; also used as the
                 window background behind the running Flutter UI. -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Required by the Flutter tool to generate GeneratedPluginRegistrant. -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>

        <!-- Disable Firebase Analytics auto data collection until the user opts in.
             Remove these two lines if you want Analytics on by default. -->
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false"/>
        <meta-data
            android:name="google_analytics_default_allow_analytics_storage"
            android:value="false"/>

    </application>

    <!-- ==========================================================
         Package visibility queries (required on Android 11+)
         Without these, plugins like image_picker, url_launcher, and
         the Flutter PROCESS_TEXT integration silently fail to find apps.
         ========================================================== -->
    <queries>
        <!-- Flutter engine — io.flutter.plugin.text.ProcessTextPlugin -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>

        <!-- image_picker — pick image from gallery -->
        <intent>
            <action android:name="android.intent.action.GET_CONTENT"/>
            <data android:mimeType="image/*"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.PICK"/>
            <data android:mimeType="image/*"/>
        </intent>

        <!-- image_picker — capture from camera (no CAMERA permission needed for the intent) -->
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE"/>
        </intent>

        <!-- url_launcher / generic browser & email links -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="mailto"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
    </queries>
</manifest>
